<section id="education" class="education-section">
  <div class="education-glow"></div>

  <div class="education-container">
    <div class="education-heading">
      <h2>Education</h2>

      <div class="education-line"></div>

      <h3>
        Academic Foundation & <br />
        Continuous Learning
      </h3>

      <p>
        A finance and accounting background supported by continuous learning in
        data analytics, Power BI, SQL, and business intelligence.
      </p>
    </div>

    <div class="education-grid">
      <div class="education-main-card">
        <div class="education-icon">🎓</div>

        <h4>Helwan University</h4>

        <span>Bachelor of Accounting — 2023</span>

        <p>
          Graduated from the Faculty of Commerce with a foundation in accounting,
          finance, business administration, financial reporting, and business
          decision-making.
        </p>
      </div>

      <div class="education-side-cards">
        <div class="education-small-card cyan">
          <div class="small-icon">📊</div>
          <div>
            <h4>Power BI</h4>
            <p>Dashboard creation, report design, KPI tracking, and visual storytelling.</p>
          </div>
        </div>

        <div class="education-small-card green">
          <div class="small-icon">🧠</div>
          <div>
            <h4>Data Modeling & DAX</h4>
            <p>Semantic models, relationships, measures, and analytical calculations.</p>
          </div>
        </div>

        <div class="education-small-card purple">
          <div class="small-icon">🗄️</div>
          <div>
            <h4>SQL & Data Preparation</h4>
            <p>Querying, transforming, cleaning, and preparing data for reporting.</p>
          </div>
        </div>

        <div class="education-small-card blue">
          <div class="small-icon">📈</div>
          <div>
            <h4>Business Intelligence</h4>
            <p>Turning structured data into clear insights for better decisions.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .education-section {
    position: relative;
    padding: 120px 24px;
    overflow: hidden;
  }

  .education-glow {
    position: absolute;
    top: 80px;
    left: 10%;
    width: 680px;
    height: 680px;
    border-radius: 999px;
    background:
      radial-gradient(circle, rgba(168, 85, 247, 0.13), transparent 65%),
      radial-gradient(circle, rgba(34, 211, 238, 0.08), transparent 70%);
    filter: blur(42px);
    pointer-events: none;
  }

  .education-container {
    position: relative;
    z-index: 1;
    max-width: 1180px;
    margin: 0 auto;
  }

  .education-heading {
    text-align: center;
    margin-bottom: 70px;
  }

  .education-heading h2 {
    margin: 0;
    color: white;
    font-size: clamp(3rem, 6vw, 5.8rem);
    line-height: 1;
    font-weight: 950;
    letter-spacing: -0.06em;
  }

  .education-line {
    width: 88px;
    height: 3px;
    margin: 28px auto;
    border-radius: 999px;
    background: linear-gradient(90deg, transparent, #a855f7, transparent);
    box-shadow: 0 0 30px rgba(168, 85, 247, 0.75);
  }

  .education-heading h3 {
    margin: 0;
    color: white;
    font-size: clamp(2rem, 4vw, 3.3rem);
    line-height: 1.12;
    font-weight: 900;
    letter-spacing: -0.04em;
  }

  .education-heading p {
    max-width: 720px;
    margin: 26px auto 0;
    color: #94a3b8;
    font-size: 17px;
    line-height: 1.9;
  }

  .education-grid {
    display: grid;
    grid-template-columns: 1.05fr 0.9fr;
    gap: 34px;
    align-items: stretch;
  }

  .education-main-card,
  .education-small-card {
    background:
      linear-gradient(145deg, rgba(15, 23, 42, 0.88), rgba(15, 23, 42, 0.45));
    border-radius: 32px;
    box-shadow:
      0 30px 90px rgba(0, 0, 0, 0.35),
      inset 0 1px 0 rgba(255, 255, 255, 0.04);
    backdrop-filter: blur(24px);
  }

  .education-main-card {
    padding: 44px;
  }

  .education-icon {
    width: 64px;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 28px;
    border-radius: 22px;
    background: rgba(168, 85, 247, 0.12);
    font-size: 28px;
  }

  .education-main-card h4 {
    margin: 0;
    color: white;
    font-size: 34px;
    font-weight: 900;
    letter-spacing: -0.04em;
  }

  .education-main-card span {
    display: block;
    margin-top: 14px;
    color: #a855f7;
    font-size: 18px;
    font-weight: 800;
  }

  .education-main-card p {
    margin-top: 30px;
    color: #cbd5e1;
    font-size: 17px;
    line-height: 1.9;
  }

  .education-side-cards {
    display: grid;
    gap: 22px;
  }

  .education-small-card {
    display: grid;
    grid-template-columns: 76px 1fr;
    gap: 22px;
    align-items: center;
    padding: 28px 32px;
    transition: 0.25s ease;
  }

  .education-small-card:hover {
    transform: translateY(-6px);
  }

  .small-icon {
    width: 66px;
    height: 66px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 22px;
    font-size: 28px;
  }

  .cyan .small-icon { background: rgba(34, 211, 238, 0.12); }
  .green .small-icon { background: rgba(74, 222, 128, 0.12); }
  .purple .small-icon { background: rgba(168, 85, 247, 0.12); }
  .blue .small-icon { background: rgba(59, 130, 246, 0.12); }

  .education-small-card h4 {
    margin: 0 0 8px;
    color: white;
    font-size: 21px;
    font-weight: 900;
  }

  .education-small-card p {
    margin: 0;
    color: #cbd5e1;
    line-height: 1.7;
  }

  @media (max-width: 980px) {
    .education-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .education-section {
      padding: 90px 20px;
    }

    .education-main-card {
      padding: 30px;
    }

    .education-small-card {
      grid-template-columns: 1fr;
      padding: 28px;
    }
  }
</style>
