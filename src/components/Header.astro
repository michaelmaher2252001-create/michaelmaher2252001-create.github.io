<section class="hero-section">
  <div class="hero-glow"></div>

  <div class="hero-content">
    <span class="hero-badge">
      Data & Financial Analyst
    </span>

    <h1>
      Hello, I’m Michael Maher.
    </h1>

    <p class="hero-subtitle">
      I turn financial and business data into clear, actionable insights.
    </p>

    <p class="hero-description">
      I build Power BI dashboards, financial reports, and analytical views that
      help businesses understand performance, monitor risk, and make smarter
      decisions.
    </p>

    <div class="hero-actions">
      <a href="https://wa.me/201013559339" target="_blank">WhatsApp</a>
      <a href="mailto:michael.maher225200@gmail.com">Email</a>
      <a href="https://www.linkedin.com/in/michael-maher-3104ab216" target="_blank">LinkedIn</a>
      <a href="https://github.com/michaelmaher2252001-create" target="_blank">GitHub</a>
      <a href="/Michael_Maher_CV.pdf" download class="primary">Download CV</a>
    </div>

    <a href="#about" class="scroll-link">
      Scroll to About ↓
    </a>
  </div>
</section>

<style>
  .hero-section {
    position: relative;
    min-height: calc(100vh - 96px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 90px 24px;
    overflow: hidden;
    text-align: center;
  }

  .hero-glow {
    position: absolute;
    width: 520px;
    height: 520px;
    border-radius: 999px;
    background: radial-gradient(circle, rgba(74, 222, 128, 0.15), transparent 65%);
    filter: blur(8px);
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 920px;
    margin: 0 auto;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 28px;
    padding: 10px 16px;
    border-radius: 999px;
    color: #4ade80;
    background: rgba(74, 222, 128, 0.1);
    border: 1px solid rgba(74, 222, 128, 0.25);
    font-size: 14px;
    font-weight: 700;
  }

  .hero-content h1 {
    margin: 0;
    color: white;
    font-size: clamp(3rem, 7vw, 6.5rem);
    line-height: 0.95;
    font-weight: 950;
    letter-spacing: -0.07em;
  }

  .hero-subtitle {
    max-width: 820px;
    margin: 28px auto 0;
    color: #e2e8f0;
    font-size: clamp(1.25rem, 2vw, 1.8rem);
    line-height: 1.45;
    font-weight: 600;
  }

  .hero-description {
    max-width: 720px;
    margin: 20px auto 0;
    color: #94a3b8;
    font-size: 17px;
    line-height: 1.9;
  }

  .hero-actions {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 38px;
  }

  .hero-actions a {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 46px;
    padding: 0 18px;
    border-radius: 999px;
    color: #cbd5e1;
    background: rgba(255, 255, 255, 0.045);
    border: 1px solid rgba(255, 255, 255, 0.11);
    font-size: 14px;
    font-weight: 700;
    transition: 0.2s ease;
  }

  .hero-actions a:hover {
    color: white;
    border-color: rgba(74, 222, 128, 0.5);
    background: rgba(74, 222, 128, 0.08);
    transform: translateY(-2px);
  }

  .hero-actions .primary {
    background: #4ade80;
    color: #020617;
    border-color: #4ade80;
  }

  .hero-actions .primary:hover {
    background: #86efac;
    color: #020617;
  }

  .scroll-link {
    display: inline-flex;
    margin-top: 34px;
    color: #64748b;
    font-size: 14px;
    transition: 0.2s ease;
  }

  .scroll-link:hover {
    color: #e2e8f0;
  }

  @media (max-width: 640px) {
    .hero-section {
      padding: 70px 20px;
      min-height: auto;
    }

    .hero-actions a {
      width: 100%;
    }
  }
</style>
