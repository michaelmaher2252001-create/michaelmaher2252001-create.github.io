<section class="hero-section">

  <div class="hero-glow"></div>

  <div class="hero-container">

    <div class="hero-badge">
      Data & Financial Analyst
    </div>

    <h1 class="hero-title">
      Hello, I’m Michael Maher.
    </h1>

    <h2 class="hero-subtitle">
      I turn financial and business data into clear, actionable insights.
    </h2>

    <p class="hero-description">
      I build Power BI dashboards, financial reports, and analytical views
      that help businesses understand performance, monitor risk,
      and make smarter decisions.
    </p>

    <div class="hero-buttons">
      <a href="https://wa.me/201013559339" target="_blank" class="hero-btn secondary">
        WhatsApp
      </a>

      <a href="mailto:michael.maher225200@gmail.com" class="hero-btn secondary">
        Email
      </a>

      <a href="https://www.linkedin.com/in/michael-maher-3104ab216" target="_blank" class="hero-btn secondary">
        LinkedIn
      </a>

      <a href="https://github.com/michaelmaher2252001-create" target="_blank" class="hero-btn secondary">
        GitHub
      </a>

      <a href="/Michael_Maher_CV.pdf" class="hero-btn primary">
        Download CV
      </a>
    </div>

  </div>

</section>

<style>
  .hero-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 120px 24px;
    overflow: hidden;
  }

  .hero-glow {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at center, rgba(34, 211, 238, 0.15), transparent 40%);
    pointer-events: none;
  }

  .hero-container {
    position: relative;
    z-index: 1;
    max-width: 1100px;
    margin: 0 auto;
    text-align: center;
  }

  .hero-badge {
    display: inline-flex;
    padding: 12px 24px;
    border-radius: 999px;
    background: rgba(74, 222, 128, 0.08);
    border: 1px solid rgba(74, 222, 128, 0.2);
    color: #4ade80;
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 36px;
  }

  .hero-title {
    margin: 0;
    color: white;
    font-size: clamp(3.6rem, 7vw, 5.5rem);
    line-height: 0.95;
    font-weight: 950;
    letter-spacing: -0.07em;
    text-wrap: balance;
  }

  .hero-subtitle {
    margin: 42px auto 0;
    max-width: 900px;
    color: white;
    font-size: clamp(1.7rem, 3.2vw, 2.4rem);
    line-height: 1.25;
    font-weight: 900;
    letter-spacing: -0.04em;
  }

  .hero-description {
    max-width: 820px;
    margin: 34px auto 0;
    color: #94a3b8;
    font-size: 20px;
    line-height: 1.9;
  }

  .hero-buttons {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 18px;
    margin-top: 56px;
  }

  .hero-btn {
    padding: 16px 30px;
    border-radius: 999px;
    text-decoration: none;
    font-weight: 700;
    transition: 0.25s ease;
  }

  .hero-btn.secondary {
    background: rgba(15, 23, 42, 0.7);
    border: 1px solid rgba(255, 255, 255, 0.08);
    color: white;
  }

  .hero-btn.primary {
    background: linear-gradient(135deg, #4ade80, #22c55e);
    color: #04130a;
    box-shadow: 0 10px 40px rgba(74, 222, 128, 0.3);
  }

  .hero-btn:hover {
    transform: translateY(-4px);
  }

  @media (max-width: 768px) {
    .hero-section {
      padding: 90px 20px;
    }

    .hero-description {
      font-size: 17px;
    }

    .hero-btn {
      width: 100%;
    }
  }
</style>
