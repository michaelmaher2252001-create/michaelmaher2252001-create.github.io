<section id="about" class="about-section">
  <div class="about-glow"></div>

  <div class="about-container">

    <!-- TITLE -->
    <div class="about-heading">
      <h2>
        About <span>Me</span>
      </h2>

      <div class="heading-line"></div>

      <h3>
        Turning Data Into <br />
        Strategic Decisions
      </h3>

      <p>
        I combine financial knowledge, business understanding, and analytics
        to transform raw numbers into clear insights for better decisions.
      </p>
    </div>

    <!-- CONTENT -->
    <div class="about-grid">

      <!-- LEFT CARD -->
      <div class="about-main-card">
        <div class="about-card-icon">
          👤
        </div>

        <h4>
          Who I Am
        </h4>

        <div class="mini-line"></div>

        <p>
          I’m a Data & Financial Analyst based in Cairo with a background in
          accounting, taxation, financial reporting, and business analytics.
        </p>

        <p>
          My experience helps me understand both the financial side and the
          business side of performance, reporting, and decision-making.
        </p>

        <p>
          I focus on building Power BI dashboards, financial reports, and
          analytical views that make complex data easier to understand.
        </p>
      </div>

      <!-- RIGHT CARDS -->
      <div class="about-side-cards">

        <div class="about-small-card cyan">
          <div class="small-icon">
            📊
          </div>

          <div>
            <h4>Data Analytics</h4>
            <p>
              Power BI dashboards, KPI reporting, SQL analysis,
              and Excel-based reporting.
            </p>
          </div>
        </div>

        <div class="about-small-card green">
          <div class="small-icon">
            💰
          </div>

          <div>
            <h4>Financial Analysis</h4>
            <p>
              Reporting, profitability review, tax exposure,
              variance analysis, and business evaluation.
            </p>
          </div>
        </div>

        <div class="about-small-card purple">
          <div class="small-icon">
            🚀
          </div>

          <div>
            <h4>Business Impact</h4>
            <p>
              Turning findings into clear recommendations that support
              decisions and performance improvement.
            </p>
          </div>
        </div>

      </div>

    </div>
  </div>
</section>

<style>
  .about-section {
    position: relative;
    padding: 120px 24px;
    overflow: hidden;
  }

  .about-glow {
    position: absolute;
    top: 80px;
    left: 50%;
    width: 700px;
    height: 700px;
    transform: translateX(-50%);
    border-radius: 999px;
    background:
      radial-gradient(circle, rgba(56, 189, 248, 0.14), transparent 65%),
      radial-gradient(circle, rgba(74, 222, 128, 0.08), transparent 70%);
    filter: blur(40px);
    pointer-events: none;
  }

  .about-container {
    position: relative;
    z-index: 1;
    max-width: 1180px;
    margin: 0 auto;
  }

  .about-heading {
    text-align: center;
    margin-bottom: 70px;
  }

  .about-heading h2 {
    margin: 0;
    color: white;
    font-size: clamp(3rem, 6vw, 5.8rem);
    line-height: 1;
    font-weight: 950;
    letter-spacing: -0.06em;
  }

  .about-heading h2 span {
    color: #22d3ee;
  }

  .heading-line {
    width: 88px;
    height: 3px;
    margin: 28px auto;
    border-radius: 999px;
    background: linear-gradient(90deg, transparent, #22d3ee, transparent);
    box-shadow: 0 0 30px rgba(34, 211, 238, 0.75);
  }

  .about-heading h3 {
    margin: 0;
    color: white;
    font-size: clamp(2rem, 4vw, 3.3rem);
    line-height: 1.12;
    font-weight: 900;
    letter-spacing: -0.04em;
  }

  .about-heading p {
    max-width: 720px;
    margin: 26px auto 0;
    color: #94a3b8;
    font-size: 17px;
    line-height: 1.9;
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1.05fr 0.9fr;
    gap: 34px;
    align-items: stretch;
  }

  .about-main-card,
  .about-small-card {
    background:
      linear-gradient(145deg, rgba(15, 23, 42, 0.88), rgba(15, 23, 42, 0.45));
    border-radius: 32px;
    box-shadow:
      0 30px 90px rgba(0, 0, 0, 0.35),
      inset 0 1px 0 rgba(255, 255, 255, 0.04);
    backdrop-filter: blur(24px);
  }

  .about-main-card {
    padding: 44px;
    min-height: 520px;
  }

  .about-card-icon {
    width: 64px;
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 28px;
    border-radius: 22px;
    background: rgba(34, 211, 238, 0.12);
    font-size: 28px;
  }

  .about-main-card h4 {
    margin: 0;
    color: white;
    font-size: 32px;
    font-weight: 900;
    letter-spacing: -0.04em;
  }

  .mini-line {
    width: 46px;
    height: 3px;
    margin: 18px 0 34px;
    border-radius: 999px;
    background: #22d3ee;
    box-shadow: 0 0 24px rgba(34, 211, 238, 0.8);
  }

  .about-main-card p {
    margin: 0 0 26px;
    color: #cbd5e1;
    font-size: 17px;
    line-height: 1.9;
  }

  .about-side-cards {
    display: grid;
    gap: 24px;
  }

  .about-small-card {
    position: relative;
    display: grid;
    grid-template-columns: 82px 1fr;
    gap: 24px;
    align-items: center;
    padding: 30px 34px;
    overflow: hidden;
    transition: 0.25s ease;
  }

  .about-small-card:hover {
    transform: translateY(-6px);
  }

  .about-small-card::before {
    content: "";
    position: absolute;
    inset: 0 auto 0 0;
    width: 4px;
    border-radius: 999px;
  }

  .about-small-card.cyan::before {
    background: #22d3ee;
    box-shadow: 0 0 30px rgba(34, 211, 238, 0.75);
  }

  .about-small-card.green::before {
    background: #4ade80;
    box-shadow: 0 0 30px rgba(74, 222, 128, 0.75);
  }

  .about-small-card.purple::before {
    background: #a855f7;
    box-shadow: 0 0 30px rgba(168, 85, 247, 0.75);
  }

  .small-icon {
    width: 72px;
    height: 72px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 22px;
    font-size: 30px;
  }

  .cyan .small-icon {
    background: rgba(34, 211, 238, 0.12);
  }

  .green .small-icon {
    background: rgba(74, 222, 128, 0.12);
  }

  .purple .small-icon {
    background: rgba(168, 85, 247, 0.12);
  }

  .about-small-card h4 {
    margin: 0 0 10px;
    color: white;
    font-size: 22px;
    font-weight: 900;
    letter-spacing: -0.03em;
  }

  .about-small-card p {
    margin: 0;
    color: #cbd5e1;
    line-height: 1.7;
    font-size: 16px;
  }

  @media (max-width: 980px) {
    .about-grid {
      grid-template-columns: 1fr;
    }

    .about-main-card {
      min-height: auto;
    }
  }

  @media (max-width: 640px) {
    .about-section {
      padding: 90px 20px;
    }

    .about-main-card {
      padding: 30px;
    }

    .about-small-card {
      grid-template-columns: 1fr;
      padding: 28px;
    }
  }
</style>
